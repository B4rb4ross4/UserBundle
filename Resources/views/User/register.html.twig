{% extends {{ base_view }} %}

{% form_theme form ':Form:bootstrap_3_horizontal_layout_improved.html.twig' %}

{% block title %}
    Registration
{% endblock %}

{% block body %}
<div class="row">
    <div class="col-lg-12">
        <h1>Registration</h1>

        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class="btn btn-default">Register</button>
        {{ form_end(form) }}
    </div>
</div>
{% endblock %}
