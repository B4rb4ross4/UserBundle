{% extends base_view %}

{% block title %}
    {% trans %}user.create.title{% endtrans %}
{% endblock %}

{% block body %}
<div class="row">
    <div class="col-lg-12">
        <h1>{% trans %}user.create.title{% endtrans %}</h1>

        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class="btn btn-default">{% trans %}user.create.action{% endtrans %}</button>
        {{ form_end(form) }}
    </div>
</div>
{% endblock %}
